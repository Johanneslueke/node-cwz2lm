{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": " cd intervals && npm install && npm start"
  },
  "stackblitz": {
    "startCommand": "npm start"
  },
  "devDependencies": {
    "@angular/cli": "^15.1.3"
  }
}
